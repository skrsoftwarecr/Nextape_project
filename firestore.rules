rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Regla base: Solo el dueño puede leer/escribir su perfil y habilidades
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
    match /skills/{userId} {
      allow read: if request.auth != null;
      allow write: if request.auth != null && request.auth.uid == userId;
    }
    // Sesiones de evaluación privadas
    match /assessments/{id} {
      allow read, write: if request.auth != null && (resource == null || resource.data.userId == request.auth.uid);
    }
    // Trabajos son públicos para lectura
    match /jobs/{id} {
      allow read: if request.auth != null;
      allow write: if false; 
    }
    // Compatibilidad privada por UID
    match /compatibility/{id} {
      allow read: if request.auth != null && id.split('_')[0] == request.auth.uid;
    }
    // Digital Twin privado
    match /digitalTwin/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }
  }
}
